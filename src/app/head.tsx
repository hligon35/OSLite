export default function Head() {
  return (
    <>
      {/* Extra cross-browser + privacy hints (does not affect layout). */}
      <meta httpEquiv="x-ua-compatible" content="IE=edge" />
      <meta name="format-detection" content="telephone=no" />
    </>
  );
}
